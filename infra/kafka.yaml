apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  sources:
    - repoURL: https://github.com/23120049/yas-helm.git
      targetRevision: vibebranch
      path: deploy/kafka/kafka-cluster
      helm:
        valueFiles:
          - $values/values/infra/kafka.yaml
    - repoURL: https://github.com/23120049/yas-gitops.git
      targetRevision: vibebranch
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
