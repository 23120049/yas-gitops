apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: payment-dev
  namespace: observability
  labels:
    app.kubernetes.io/instance: payment-dev
    app.kubernetes.io/name: payment
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: payment-dev
      app.kubernetes.io/name: payment
  namespaceSelector:
    matchNames:
      - dev
  podMetricsEndpoints:
    - port: metric
      path: /actuator/prometheus
      interval: 30s
      scheme: http
      honorLabels: true
