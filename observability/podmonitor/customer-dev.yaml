apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: customer-dev
  namespace: observability
  labels:
    app.kubernetes.io/instance: customer-dev
    app.kubernetes.io/name: customer
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: customer-dev
      app.kubernetes.io/name: customer
  namespaceSelector:
    matchNames:
      - dev
  podMetricsEndpoints:
    - port: metric
      path: /actuator/prometheus
      interval: 30s
      scheme: http
      honorLabels: true
