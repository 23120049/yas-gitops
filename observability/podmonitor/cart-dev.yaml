apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cart-dev
  namespace: observability
  labels:
    app.kubernetes.io/instance: cart-dev
    app.kubernetes.io/name: cart
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: cart-dev
      app.kubernetes.io/name: cart
  namespaceSelector:
    matchNames:
      - dev
  podMetricsEndpoints:
    - port: metric
      path: /actuator/prometheus
      interval: 30s
      scheme: http
      honorLabels: true
