elasticsearch:
  replicas: 1
  resources:
    requests:
      memory: "1Gi"
      cpu: "500m"
    limits:
      memory: "1Gi"
      cpu: "1000m"
kibana:
  ingress:
    enabled: false
    hostname: kibana.yas.local.com
