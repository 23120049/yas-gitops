apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-prometheus-scrape-order
  namespace: dev
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: order
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - "cluster.local/ns/observability/sa/prometheus-kube-prometheus-prometheus"
      to:
        - operation:
            paths:
              - /order/actuator
              - /order/actuator/*
              - /actuator
              - /actuator/*
              - /order/prometheus
              - /prometheus